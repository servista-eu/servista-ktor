---
phase: 07-service-scaffold-template
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - templates/Dockerfile
  - templates/k8s/deployment.yaml
  - templates/k8s/service.yaml
  - templates/k8s/configmap.yaml
  - templates/application.conf
  - templates/db/migration/V1__initial_schema.sql
autonomous: true
requirements:
  - FOUND-06
must_haves:
  truths:
    - "New service developers can copy template files as starting points without modification to the library"
    - "Dockerfile produces a working container image with OTel Java Agent attached via JAVA_TOOL_OPTIONS"
    - "Kubernetes manifests include startup, liveness, and readiness probes pointing to /health/* endpoints"
    - "Reference application.conf shows the complete servista {} HOCON structure with env var substitution"
    - "Example migration demonstrates RLS policy pattern with ENABLE and FORCE ROW LEVEL SECURITY"
  artifacts:
    - path: "templates/Dockerfile"
      provides: "Three-stage build with OTel agent download"
      contains: "JAVA_TOOL_OPTIONS"
    - path: "templates/k8s/deployment.yaml"
      provides: "Kubernetes Deployment with all three probe types"
      contains: "/health/startup"
    - path: "templates/k8s/service.yaml"
      provides: "ClusterIP Service exposing port 8080"
    - path: "templates/application.conf"
      provides: "Reference HOCON with all servista.* keys and env var substitution, including kafka.consumer section"
      contains: "servista.database, servista.kafka.consumer"
    - path: "templates/db/migration/V1__initial_schema.sql"
      provides: "Example migration with RLS policy pattern"
      contains: "FORCE ROW LEVEL SECURITY"
  key_links:
    - from: "templates/Dockerfile"
      to: "OTel Java Agent 2.20.0"
      via: "GitHub release download in build stage"
      pattern: "opentelemetry-javaagent"
    - from: "templates/k8s/deployment.yaml"
      to: "templates/Dockerfile health endpoints"
      via: "httpGet probes to /health/*"
      pattern: "/health/startup|/health/live|/health/ready"
    - from: "templates/application.conf"
      to: "ServistaConfig.kt"
      via: "HOCON keys matching config data class fields including kafka.consumer"
      pattern: "servista\\.database|servista\\.kafka|servista\\.kafka\\.consumer|servista\\.logging"
---

<objective>
Create reference template files for bootstrapping new Servista service repositories.

Purpose: Provide copy-paste starting points for the Dockerfile, Kubernetes manifests, HOCON configuration, and database migrations that every new service will need. These are NOT auto-applied by the library -- they live in a `templates/` directory within the servista-ktor repo and developers copy them when creating new services.

Output: Complete set of reference files in `templates/` directory covering containerization, Kubernetes deployment, configuration, and database migration patterns.
</objective>

<execution_context>
@/home/sven/.claude-pp/get-shit-done/workflows/execute-plan.md
@/home/sven/.claude-pp/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-service-scaffold-template/07-CONTEXT.md
@.planning/phases/07-service-scaffold-template/07-RESEARCH.md
@.planning/phases/07-service-scaffold-template/01-01-SUMMARY.md

<interfaces>
<!-- These templates reference concepts from servista-ktor but are standalone files -->

Health endpoints provided by servista-ktor:
- GET /health/startup  -> Kubernetes startupProbe
- GET /health/live     -> Kubernetes livenessProbe
- GET /health/ready    -> Kubernetes readinessProbe
- GET /metrics         -> Prometheus scrape endpoint

HOCON config keys from ServistaConfig:
- servista.database.url, servista.database.user, servista.database.password
- servista.kafka.brokers, servista.kafka.schema-registry-url
- servista.kafka.consumer.group-id, servista.kafka.consumer.auto-offset-reset
- servista.logging.requests.enabled, .level, .exclude-paths
- servista.health (no special config needed)

OTel environment variables:
- OTEL_SERVICE_NAME (per-service, set in Deployment)
- OTEL_EXPORTER_OTLP_ENDPOINT (OTel Collector gRPC endpoint)
- OTEL_EXPORTER_OTLP_PROTOCOL=grpc
- OTEL_TRACES_SAMPLER=parentbased_traceidratio
- OTEL_TRACES_SAMPLER_ARG=0.1 (10% sampling in production)
- SERVISTA_LOG_FORMAT=json|console

RLS pattern from servista-commons:
- Tables with organization_id MUST have ENABLE + FORCE ROW LEVEL SECURITY
- RLS policy uses current_setting('app.current_organization_id')::BIGINT
- RlsSessionManager.withOrganization() sets this session variable per request
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile and Kubernetes manifest templates</name>
  <files>
    templates/Dockerfile,
    templates/k8s/deployment.yaml,
    templates/k8s/service.yaml,
    templates/k8s/configmap.yaml
  </files>
  <action>
Create reference deployment files in the `templates/` directory at the root of the servista-ktor repo. These are copy-paste starting points with placeholder comments showing where to customize.

**templates/Dockerfile** -- Three-stage multi-stage build:

Stage 1 (`build`): Build application
- Base: `eclipse-temurin:21-jdk-alpine`
- Copy project files, run `./gradlew buildFatJar --no-daemon` (or `shadowJar` depending on what convention plugin provides -- check during implementation, Ktor plugin provides `buildFatJar`)
- Note in comment: Replace with actual build task from convention plugin

Stage 2 (`otel`): Download OTel Java Agent
- Base: `alpine:3.21`
- ARG `OTEL_AGENT_VERSION=2.20.0`
- Download from `https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/download/v${OTEL_AGENT_VERSION}/opentelemetry-javaagent.jar`

Stage 3 (`runtime`): Runtime image
- Base: `eclipse-temurin:21-jre-alpine`
- Copy OTel agent from `otel` stage to `/opt/opentelemetry-javaagent.jar`
- Copy application JAR from `build` stage
- Set `JAVA_TOOL_OPTIONS="-javaagent:/opt/opentelemetry-javaagent.jar"` -- using JAVA_TOOL_OPTIONS instead of modifying ENTRYPOINT makes it easy to disable the agent for debugging
- Set default OTel environment variables:
  - `OTEL_EXPORTER_OTLP_ENDPOINT="http://otel-collector:4317"`
  - `OTEL_EXPORTER_OTLP_PROTOCOL="grpc"`
  - `OTEL_TRACES_SAMPLER="parentbased_traceidratio"`
  - `OTEL_TRACES_SAMPLER_ARG="0.1"` (10% sampling)
  - `SERVISTA_LOG_FORMAT="json"` (production default)
- Comment: `OTEL_SERVICE_NAME` set per-service in Kubernetes Deployment
- Expose port 8080
- ENTRYPOINT: `java -jar /app/app.jar`
- Add comment block at top explaining what to customize: service name in OTEL_SERVICE_NAME, build task, JAR path

**templates/k8s/deployment.yaml** -- Kubernetes Deployment:
- `apiVersion: apps/v1`, `kind: Deployment`
- Metadata name: `my-service` (placeholder, customize per service)
- Single replica (adjust for production)
- Container:
  - Image: `registry.example.com/my-service:latest` (placeholder)
  - Port: 8080
  - Environment variables:
    - `OTEL_SERVICE_NAME: "my-service"` (customize)
    - `OTEL_EXPORTER_OTLP_ENDPOINT` from ConfigMap
    - `SERVISTA_DATABASE_URL`, `SERVISTA_DATABASE_USER`, `SERVISTA_DATABASE_PASSWORD` from Secret
    - `SERVISTA_KAFKA_BROKERS` from ConfigMap
  - startupProbe: httpGet `/health/startup` port 8080, initialDelaySeconds=5, periodSeconds=5, failureThreshold=30 (allows 2.5 min startup)
  - livenessProbe: httpGet `/health/live` port 8080, periodSeconds=15, failureThreshold=3
  - readinessProbe: httpGet `/health/ready` port 8080, periodSeconds=10, failureThreshold=3
  - Resources: requests cpu=200m memory=256Mi, limits cpu=1 memory=512Mi
- Add comment header explaining what to customize

**templates/k8s/service.yaml** -- ClusterIP Service:
- `apiVersion: v1`, `kind: Service`
- Type: ClusterIP
- Port 8080 -> targetPort 8080
- Selector matching deployment labels

**templates/k8s/configmap.yaml** -- Optional ConfigMap:
- `apiVersion: v1`, `kind: ConfigMap`
- Data fields for non-secret configuration:
  - `OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.observability:4317"`
  - `SERVISTA_KAFKA_BROKERS: "kafka-bootstrap.kafka:9092"`
- Comment explaining when to use ConfigMap vs Secret (secrets for credentials, configmap for endpoints)
  </action>
  <verify>
    <automated>ls -la infrastructure/servista-ktor/templates/ infrastructure/servista-ktor/templates/k8s/ 2>&amp;1</automated>
  </verify>
  <done>
    - Dockerfile exists with 3-stage build (build, otel agent download, runtime)
    - OTel agent attached via JAVA_TOOL_OPTIONS env var
    - deployment.yaml has all three Kubernetes probes (startup, liveness, readiness) pointing to /health/* endpoints
    - service.yaml exposes port 8080 as ClusterIP
    - configmap.yaml shows non-secret configuration pattern
    - All files have comment headers explaining customization points
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reference application.conf and example database migration</name>
  <files>
    templates/application.conf,
    templates/db/migration/V1__initial_schema.sql
  </files>
  <action>
**templates/application.conf** -- Complete reference HOCON configuration:
- Show the full `ktor {}` block with deployment port (8080, overridable via PORT env var) and application module reference
- Show the complete `servista {}` block with ALL configuration keys and env var substitution:
  - `servista.database.url` = localhost default + `${?SERVISTA_DATABASE_URL}` override
  - `servista.database.user` = default + `${?SERVISTA_DATABASE_USER}` override
  - `servista.database.password` = default + `${?SERVISTA_DATABASE_PASSWORD}` override
  - `servista.kafka.brokers` = localhost:9092 default + `${?SERVISTA_KAFKA_BROKERS}` override
  - `servista.kafka.schema-registry-url` = localhost:8081 default + `${?SERVISTA_KAFKA_SCHEMA_REGISTRY_URL}` override
  - `servista.kafka.consumer.group-id` = "my-service" default + `${?SERVISTA_KAFKA_CONSUMER_GROUP_ID}` override (comment: set to service name, unique per service)
  - `servista.kafka.consumer.auto-offset-reset` = "earliest" default + `${?SERVISTA_KAFKA_CONSUMER_AUTO_OFFSET_RESET}` override (comment: "earliest" for new consumers, "latest" for high-volume topics)
  - `servista.logging.requests.enabled` = true + `${?SERVISTA_LOG_REQUESTS_ENABLED}` override
  - `servista.logging.requests.level` = "INFO" + `${?SERVISTA_LOG_REQUESTS_LEVEL}` override
  - `servista.logging.requests.exclude-paths` = ["/health"]
  - `servista.health {}` block (no special config, included for documentation)
- Use HOCON's environment variable substitution pattern: set default first, then override on next line with `${?VAR}` syntax (standard Ktor pattern)
- Include comment block at top explaining:
  - Single config file per service (no profile-based files)
  - All environment differences come from env vars via Kubernetes ConfigMaps/Secrets
  - Localhost defaults are for local development with Testcontainers
  - Services extend with their own HOCON sections for service-specific config
- Include example `myservice {}` section showing how services add custom config

**templates/db/migration/V1__initial_schema.sql** -- Example Flyway migration demonstrating RLS:
- Create an example `example_entity` table with:
  - `id BIGINT PRIMARY KEY` (Snowflake ID from servista-commons)
  - `organization_id BIGINT NOT NULL` (tenant scope)
  - `name TEXT NOT NULL`
  - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
  - `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`
- Enable RLS: `ALTER TABLE example_entity ENABLE ROW LEVEL SECURITY`
- Create tenant isolation policy: `CREATE POLICY tenant_isolation ON example_entity USING (organization_id = current_setting('app.current_organization_id')::BIGINT)`
- Force RLS even for table owner: `ALTER TABLE example_entity FORCE ROW LEVEL SECURITY` -- **CRITICAL:** Without FORCE, the table owner bypasses RLS
- Add comment block explaining:
  - Every table with `organization_id` MUST follow this pattern
  - `app.current_organization_id` is set by `RlsSessionManager.withOrganization()` from servista-commons
  - Without FORCE ROW LEVEL SECURITY, the table owner (which is often the application's DB user) bypasses the policy entirely
  - Flyway migration files go in `src/main/resources/db/migration/` in each service
  - Naming convention: `V{number}__{description}.sql` (double underscore)
  </action>
  <verify>
    <automated>ls -la infrastructure/servista-ktor/templates/ infrastructure/servista-ktor/templates/db/migration/ 2>&amp;1</automated>
  </verify>
  <done>
    - application.conf shows complete servista {} HOCON structure with all config keys including kafka.consumer (group-id, auto-offset-reset)
    - Every config value has a localhost default plus ${?ENV_VAR} override for Kubernetes
    - Consumer config section includes comments explaining group-id should be unique per service
    - Example service-specific config section demonstrates extensibility
    - V1__initial_schema.sql creates table with organization_id and full RLS pattern
    - ENABLE + FORCE ROW LEVEL SECURITY both present with explanatory comments
    - Comment blocks explain the patterns and customization points
  </done>
</task>

</tasks>

<verification>
- All template files exist in `templates/` directory
- Dockerfile has 3 stages and OTel agent via JAVA_TOOL_OPTIONS
- Kubernetes manifests reference correct health endpoints
- application.conf keys match ServistaConfig data class fields, including kafka.consumer section
- Migration SQL includes both ENABLE and FORCE ROW LEVEL SECURITY
- All files have comment headers explaining how to customize
</verification>

<success_criteria>
- Complete set of reference templates for new service bootstrapping
- Dockerfile produces container image with OTel Java Agent auto-instrumentation
- Kubernetes manifests have all three probe types configured correctly
- Reference HOCON configuration documents every servista.* config key with env var substitution, including kafka.consumer (groupId, autoOffsetReset)
- Example migration demonstrates mandatory RLS pattern
- Templates are self-documented with comments explaining customization
</success_criteria>

<output>
After completion, create `.planning/phases/07-service-scaffold-template/01-03-SUMMARY.md`
</output>
